provider: cloudflare

cloudflare:
  apiToken:
    existingSecret: external-dns
    key: CF_API_TOKEN

domainFilters:
  - "tondomaine.com"

policy: sync

sources:
  - ingress

txtOwnerId: k8s-traefik

logLevel: info
